#include "sort.h"
/**
* cocktail_sort_list - shell sorts doubly linked list 
* @list:  each time you swap two elements listed
*/
void cocktail_sort_list(listint_t **list)
{
if (!list || !(*list) || !(*list)->next)
return;
}
